 <h1>Tags Index</h1> 
  Tags:   <% @tags.each do |tag|  %>
            <%= link_to(
                  tag.name,
                  products_path(tag: tag.name),
                  class: "badge badge-pill badge-primary"
                  ) %>
          <% end %>